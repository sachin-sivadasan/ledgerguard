name: ledgerguard_flutter
description: LedgerGuard Revenue Intelligence Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  provider: ^6.1.0
  fl_chart: ^0.69.0
  intl: ^0.19.0
  google_fonts: ^6.2.0
  mixpanel_flutter: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
